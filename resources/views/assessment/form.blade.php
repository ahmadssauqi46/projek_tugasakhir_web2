@extends('layouts.app')
@section('content')
<section class="section"><div class="container"><div class="row g-4"><div class="col-lg-8"><h2 class="fw-bold">{{ $mode=='quiz' ? 'Quiz Interaktif' : 'Evaluasi Akhir' }}</h2><span class="chip mb-3">{{ $mode=='quiz' ? '10 soal | nilai tampil dan tersimpan' : '20 soal | nilai tidak ditampilkan ke siswa' }}</span><form class="card-soft p-4" method="POST" action="{{ $mode=='quiz' ? route('quiz.submit',$module) : route('evaluasi.submit') }}">@csrf<label class="fw-bold small">Nama Siswa</label><input class="form-control mb-4" name="student_name" value="{{ session('user_name') }}" placeholder="Masukkan nama" required>@foreach($questions as $q)<div class="mb-4"><h6 class="fw-bold">Soal {{ $loop->iteration }} dari {{ $questions->count() }}</h6><p>{{ $q->question }}</p>@foreach(['A'=>'option_a','B'=>'option_b','C'=>'option_c','D'=>'option_d'] as $key=>$field)<label class="question-option d-block"><input type="radio" name="answers[{{ $q->id }}]" value="{{ $key }}" required> <b>{{ $key }}.</b> {{ $q->$field }}</label>@endforeach</div>@endforeach<button class="btn btn-main">Kirim</button></form></div><div class="col-lg-4"><div class="card-soft p-4 mt-lg-5"><h5 class="fw-bold">Aturan</h5><p class="muted">{{ $mode=='quiz' ? 'Quiz hanya dapat dikerjakan satu kali. Nilai akan masuk ke leaderboard.' : 'Evaluasi akhir dikirim ke guru. Siswa tidak melihat nilai agar fokus menyelesaikan pembelajaran.' }}</p></div></div></div></div></section>
@endsection
