@extends('layouts.app')@section('content')
<section class="hero text-center"><div class="container"><h1 class="fw-bold">Materi Pembelajaran</h1><p class="lead">Selesaikan latihan tiap modul agar modul berikutnya terbuka.</p></div></section><section class="section"><div class="container"><div class="row g-4">@foreach($modules as $i=>$m)@php $prev=$i>0?$modules[$i-1]:null; $locked=$prev && !($progress[$prev->id]??false); @endphp<div class="col-md-4"><div class="card-soft p-4 h-100 {{ $locked?'locked':'' }}"><div class="display-4">{{ $m->image }}</div><span class="badge-step">Modul {{ $m->order }}</span><h4 class="fw-bold mt-3">{{ $m->title }}</h4><p class="text-muted">{{ $m->summary }}</p>@if($locked)<button class="btn btn-secondary w-100" disabled>🔒 Terkunci</button><small class="text-muted d-block mt-2">Selesaikan latihan {{ $prev->title }} dulu.</small>@else<a href="{{ route('materi.show',$m) }}" class="btn btn-main w-100">Baca Materi</a>@endif</div></div>@endforeach</div></div></section>
@endsection
