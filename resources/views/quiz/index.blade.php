@extends('layouts.app')
@section('content')
<section class="section"><div class="container"><h2 class="fw-bold">Quiz Interaktif</h2><p class="muted">Quiz berisi 10 soal, hanya bisa dikerjakan 1 kali, dan nilai tersimpan ke database.</p><div class="row g-4">@foreach($modules as $m)<div class="col-md-6 col-xl-4"><div class="card-soft p-4 h-100"><div class="icon-box mb-3"><i class="bi {{ $m->image ?: 'bi-cpu' }}"></i></div><h5 class="fw-bold">{{ $m->title }}</h5><p class="small muted">{{ $m->summary }}</p>@if(isset($results[$m->id]))<span class="badge status-done mb-3">Sudah dikerjakan - Nilai {{ $results[$m->id] }}</span><button class="btn btn-soft btn-sm" disabled>Selesai</button>@else<a class="btn btn-main btn-sm" href="{{ route('quiz.show',$m) }}">Mulai Quiz</a>@endif</div></div>@endforeach</div></div></section>
@endsection
